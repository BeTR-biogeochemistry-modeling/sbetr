set(FAKEPARA_SOURCES
  fakeParaType.F90
)
include_directories(${CMAKE_BINARY_DIR}/src/betr/betr_util)
include_directories(${CMAKE_BINARY_DIR}/src/betr/betr_math)
include_directories(${CMAKE_BINARY_DIR}/src/io_util)
include_directories(${CMAKE_BINARY_DIR}/src/Applications/soil-farm/bgcfarm_util)
include_directories(${CMAKE_BINARY_DIR}/src/Applications/soil-farm/fake/fakePara)
include(add_betr_library)
add_betr_library(fakePara ${FAKEPARA_SOURCES})
set(BETR_LIBRARIES fakePara;${BETR_LIBRARIES} PARENT_SCOPE)
set(BETR_LIBRARIES fakePara;${BETR_LIBRARIES})
if (NOT CMAKE_INSTALL_PREFIX STREQUAL "INSTALL_DISABLED")
    install(TARGETS fakePara DESTINATION lib)
    file(GLOB HEADERS *.h)
    install(FILES ${HEADERS} DESTINATION include/soil-farm/fake/fakePara)
endif()
